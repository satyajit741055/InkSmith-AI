services:
  postgres:
    image: postgres:16
    container_name: postgress-inksmith
    environment:
      POSTGRES_USER: inksmith
      POSTGRES_PASSWORD: inksmith_123
      POSTGRES_DB: inksmith
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: